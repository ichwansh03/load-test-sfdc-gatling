spring:
  application:
    name: loadtest

salesforce:
    client:
        id: your-client-id
        secret: your-client-secret
        username: your-username
        password: your-password
        login-url: https://login.salesforce.com
        oauth2-token-url: /services/oauth2/token
        objects: Account
